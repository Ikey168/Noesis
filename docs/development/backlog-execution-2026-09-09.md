# Backlog execution — 2026-09-09

## Current disposition — non-implementation follow-up transferred

At the user's request on 2026-09-09, the remaining **23 issues were closed as not planned**, including their five tracking parents. GitHub now has **zero open issues**. Outstanding evaluations, independent human annotation/review and credentialed provider checks are preserved in [the follow-up document](further-evaluations-and-access.md), with original issue requirements and a verified closure receipt. These closures do not claim the deferred acceptance checks passed. Code remains local and uncommitted. Earlier counts below are historical snapshots.

Current GitHub state: sixteen issues closed in this date’s continuation, 23 remain open. The latest completed evaluations and outstanding acceptance requirements are recorded below. Changes remain local and uncommitted.

## Jina Reader — #1490

The opt-in adapter now verifies that the original snapshot digest, source URL, final URL and fetch time match a stored source observation before sending a URL for remote transformation. Forged attribution fails before any network request. German Berlin and English Python public pages were acquired and compared with the current local extractor using the same captured original source bytes. Jina token overlap recovers 97.54% and 71.26% of the baseline body tokens respectively, at measured provider request latencies of 1.91 and 0.38 seconds. This measures sequential captures, not independent human fidelity labels or exact word-level citation alignment. Native reported usage is 5,131 and 5,369 tokens. The unauthenticated public endpoint was used; no paid account or purchase was involved.

Both original and transformed snapshots, attribution, usage and replay are retained. Markdown explicitly has no precise source locators. Re-running the evaluator on the reopened database reuses the captures. Decision: defer production adoption; output omission and imprecise citation mapping do not justify replacing existing extraction. Reproduce with `scripts/evaluate_jina.py --network-approved --database /tmp/jina.duckdb --out /tmp/jina.json`. Evidence: `workflow-review-evidence/jina-live-2026-09-09.json`. [Official Reader documentation](https://github.com/jina-ai/reader) describes the public endpoint and remote transformation; publisher terms remain attached to original acquisitions. No private source was sent.

## Presidio — #1494

Native Presidio 2.2.364 with spaCy 3.8.16 and pinned German/English small models 3.8.0 was run on authored German/English regression documents, including names, addresses and identifiers. Exact-span precision is 0.8462 and recall 0.7857 (11 true positives, two false positives, three false negatives). These expectations are authored and do not claim independent human annotation. The originals remain unchanged. Removed text is not copied into derived metadata; sensitive source identifiers are now replaced by an opaque source-reference hash in the export payload. Restricted artifact-graph dependencies preserve the real source/revision for authorized review. The evaluation now checks actual locator IDs instead of an obsolete source-text-hash field.

A native job through OptionalAnalysisStore published a derived artifact, retained its restricted source dependency, reopened/replayed identically, and rejected revoked access. Decision: defer unattended redaction; explicit local review remains available. Missed detections mean this is not an export-safety certificate. Evidence: `workflow-review-evidence/presidio-reviewed-2026-09-09.json` and `presidio-integration-2026-09-09.json`. Reproduce the benchmark with `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_presidio.py --out /tmp/presidio.json`. Models must be installed explicitly; runtime loading does not download missing models or send documents remotely.

## CTIS — #1478

The documented public UI (portal 2.2.5) successfully supplied a 135,441-byte, 68-row search CSV for the keyword Berlin and a 5,187,150-byte trial ZIP for FIRE-12, `2025-524131-39-00`. A keyword match does not establish a Berlin trial site. The new `ctis-search-csv` import profile preserves native columns, country recruitment statuses (including commas within status text), the decision date separately from unknown registration/publication dates, and tri-state results availability separately from results evidence. Individual site coverage remains explicitly missing in this export.

The `trial-document` profile imports separately selected registration HTML, protocol, results or publication documents with a validated trial ID, stable document ID, explicit relationship and captured source locators. The live bundle supplied registration HTML (35,586 extracted characters), German protocol synopsis (22,225) and English protocol (219,978). All four imports, including the CSV, replayed identically after database reopening without a network client. The original ZIP and each selected member were hashed; no unselected member was extracted. Published redactions and publisher reuse conditions remain authoritative. No supported public REST API is assumed. Public downloads require an operator; runtime import itself is offline. PDF parsing is limited to 120 seconds and 1 GiB RSS per selected document, and files to 20 MB. The 41 focused regional tests cover changed registrations, digest mismatch, missing documents, explicit protocol/result separation, failed publication rollback and durable replay. Publication matches require explicit identifiers or the existing review workflow; no automatic identity merge occurs.

Evidence: `workflow-review-evidence/ctis-native-export-2026-09-09.json`. Access instructions: [CTIS search tips and guidance](https://euclinicaltrials.eu/search-tips-and-guidance/). Reproduce by downloading results and the selected trial through those controls, then using `python -m src.noesis_cli.optional --config CONFIG --request REQUEST --output RESULT` with `kind=registry_import`, `provider=ctis`, the local `file`, its `sha256`, and `params.format=ctis-search-csv`. For selected HTML/PDF members use `params.format=trial-document`, `trial_id`, `document_id`, `document_kind`, `title`, `language`, and the official `source_url`. The source URL is required for both profiles. Keep the downloaded archive and member hashes with the acquisition receipt. Native transport/import acceptance is complete; this does not certify clinical claims or independent model quality.

## Phoenix / MLflow — #1500

Phoenix 20.8.0 with OpenTelemetry 1.44.0 and MLflow skinny 3.16.0 were actually executed locally with telemetry disabled. Three authored operational probes (success, timeout and unsupported answer) round-tripped their status and safe correlation fields through both systems, excluding private source bodies and tokens. Phoenix median OTLP export was 4.78 ms; MLflow median run creation, logging and termination was 13.32 ms. These operation boundaries differ and are not a performance ranking. Neither minimal projection adds root-cause evidence beyond the authoritative runtime receipts. This is a transport/diagnostic comparison, not a human debugging study.

Phoenix ran on loopback with an explicit one-day retention configuration and was stopped after evaluation. MLflow used disposable local SQLite; soft deletion was verified before removal of the temporary store. Soft deletion is not evidence of physical production expiry. No hosted account or recurring service was provisioned. Decision: defer adding Phoenix as a default service; retain the opt-in, bounded exporter. Evidence: `workflow-review-evidence/phoenix-mlflow-comparison-2026-09-09.json`. Reproduce with a local Phoenix server and `MLFLOW_DISABLE_TELEMETRY=true python scripts/evaluate_phoenix.py --endpoint http://127.0.0.1:16006 --out /tmp/phoenix-comparison.json`. Install the recorded versions explicitly. The evaluator's raw receipt serialization baseline excludes durable database writes.

## DRKS — #1479

The documented public JSON export was downloaded for PAIS Berlin, `DRKS00037756`, native version 5 dated 17 April 2026. The new `drks-public-json` profile selects descriptions by their locale tags, maps recruitment status separately from record-update status, preserves the primary sponsor, registration/update dates and seven actual Berlin recruitment sites, and leaves absent results missing. Both German and English descriptions remain in the captured native payload. Explicit CTIS identifiers can enter the existing review workflow; EudraCT identifiers and other unrecognized identifiers remain distinct source metadata. No inferred study merge occurs.

The real export imported and replayed after database reopening without network access. Focused tests also cover reordered/duplicate locales, German text, missing result summaries, status distinctions, changed registrations, duplicate imports and cross-registry review. Evidence: `workflow-review-evidence/drks-native-2026-09-09.json`. Reproduce by opening the selected trial's Download tab, selecting JSON and accepting the published terms, then importing with the existing CLI using `kind=registry_import`, `provider=drks`, local `file`, `sha256`, and `params` containing `format=drks-public-json`, `language=de` and the official `source_url`. One native trial object is accepted per import, limited to 20 MB; public search/export remains an operator action, with no assumed private API.

The [DRKS usage conditions](https://www.bfarm.de/DE/Das-BfArM/Aufgaben/Deutsches-Register-Klinischer-Studien/Studie-suchen/_node.html#nutzungsbedingungen), read through the ordinary public browser, distinguish older records from records registered from 2025 or updated in 2025. This selected September 2025 registration falls under the stated CC BY 4.0 terms. Attribution to DRKS and the retrieval date are retained; this is not a blanket license for older records. The export is registration evidence, not independent validation of the study's claims.

## CELLAR — #1480

The live SPARQL endpoint required the specific SPARQL-results Accept header and typed `xsd:string` CELEX values. Both are now explicit. Four bounded German/English queries for GDPR (`32016R0679`) and the selected case-law identifier (`62014CJ0362`) returned native work/expression/manifestation/item identities, source relationships and language distinctions. Downloaded German GDPR PDF text contains 410,384 extracted characters across 1,704 source blocks; the selected English case-law HTML has 90,915 characters across 288 blocks. All six metadata/text observations replayed offline after database reopening.

Live GDPR metadata demonstrated why document date must differ from publication date: 27 April 2016 is retained as document date, while 4 May 2016 comes from the linked Official Journal's date. Both supplied effective dates (24 May 2016 and 25 May 2018) are retained; a single effective-from field stays unknown when multiple values exist. Missing case-law publication dates remain unknown. The adapter never certifies current law, and bounded query pages explicitly disclose incomplete relationship coverage. Regression tests cover changed text at the same URL, separate language manifestations, old-revision replay, explicit identifiers and multiple date values.

Evidence: `workflow-review-evidence/cellar-native-2026-09-09.json`, `cellar-documents-2026-09-09.json`, and `cellar-replay-2026-09-09.json`. The [official CELLAR knowledge-graph documentation](https://op.europa.eu/en/web/cellar/cellar-data/metadata/knowledge-graph) documents SPARQL access and item download links. Reproduce using the existing regional CLI with `provider=cellar`, `operation=cellar`, bounded `params.celex_ids`, `params.languages` and `params.limit`; select returned item URLs with `operation=acquire_document`, their language and parent work identity. Query responses are bounded to 5 MB, selected documents to 20 MB, and PDF parsing to 120 seconds/1 GiB. Requests consume the explicit durable project budget; no account or paid service was needed. This is source-acquisition acceptance, not independent legal interpretation.

## Outlines — #1520, still open

Native execution revealed memory-heavy default DFA compilation, duplicate chat templating, and an unsupported `uniqueItems` grammar keyword. The optional dependency set now pins LLGuidance 1.8.0 alongside Outlines 1.3.3. Its grammar is checked before inference; reference uniqueness remains enforced by the full post-generation schema validator, while the generation grammar still constrains references to authorized enum values. The exact single-template prompt is used for context counting. Seven report-generation tests passed with the actual optional grammar package installed.

A pinned SmolLM2-135M-Instruct worker (90-second deadline, 3 GiB RSS ceiling) now executes generation and rejects its incomplete JSON as `truncated_or_invalid_json`. Evidence: `workflow-review-evidence/outlines-native-2026-09-09.json`. No model edit is approved or published. The original default-compiler probe was killed with exit 137; that observation is not a successful benchmark. Independent substantive-edit expectations and the full unconstrained/notices comparison remain outstanding, so this issue stays open.

## Berlin public law and decisions — #1482

The public portal's documented XML download supplied the current selected Berlin Constitution as a ZIP containing `content.xml`. The new `berlin-juris-xml` profile preserves 115 native norm IDs and their individual metadata, 297 paragraph locators, enactment/effective dates, the gazette reference and the supplied amendment notice. The 59,466 extracted characters retain article identities. External DTD loading and entity expansion are disabled; unresolved entities fail. Publication date is not inferred from enactment date, and historical/current selection is explicit rather than a legal-validity certificate.

The rendered public judgment `NJRE001141408` (Verfassungsgerichtshof Berlin, 155/11, decision 13 May 2013) imported through `berlin-juris-html` with its ECLI and 68 nonempty paragraphs/46,869 characters. Locators retain paragraph numbers and distinguish disposition, reasons, official headnotes and dissent. Editorial/unclassified text stays outside the judgment evidence body. The advertised PDF link returned a 196-byte non-PDF response to the independent HTTP fetch; it was retained as an unsuccessful document-format observation, not published as PDF evidence. Both successful XML/HTML imports replay offline. Tests cover altered publications, historical flags, old-version replay, inaccessible pages, entity references and editorial exclusion.

Evidence: `workflow-review-evidence/berlin-native-2026-09-09.json`. Reproduce from the [public search](https://gesetze.berlin.de/bsbe/search) and [document controls](https://www.gesetze.berlin.de/jportal/portal/page/fshelp.psml?cmsuri=/hilfe/de/ep/weitere_suchfunktionen/dokumentenausgabe.jsp): select the actual displayed XML export or capture the rendered judgment, then use `kind=registry_import`, `provider=berlin-law`, the local file/digest and `params.format=berlin-juris-xml` or `berlin-juris-html`. Both require `source_url`; HTML additionally requires the matching `official_id`. XML/HTML input is bounded to 20 MB. Operator public acquisition remains necessary; no unrestricted API or complete court coverage is claimed. Publisher reuse conditions, anonymisation and official/editorial distinctions remain attached.

## PaddleOCR versus the existing OCR path — #1496

Native PaddleOCR 3.7.0/PaddlePaddle 3.3.1 now runs with pinned PP-OCRv5 mobile detection and Latin recognition models. The CPU oneDNN path failed with an unsupported PIR attribute, so the optional adapter explicitly disables MKLDNN and records that configuration. Other preprocessing remains disabled to preserve the rendered-page coordinate frame.

The frozen CC0 authored corpus at `tests/fixtures/ocr_benchmark/` covers German/English text, a table, a rotated page and a degraded scan. The actual existing book OCR function ran through a new bounded baseline worker using its unchanged English default, with Tesseract reporting 5.5.2 and native language data. On identical bytes, whitespace-normalized character/word error rates were 0.01389/0.08475 for Tesseract and 0.15972/0.18644 for PaddleOCR. Whole-job time/RSS were 2.06 seconds/282 MB and 11.88 seconds/1.28 GB. These are authored regression measurements, not independent real-document quality labels. Paddle's principal failure was reading order on the rotated page despite recognizing each line. Exact-match box IoU ranged from 0.82 to 0.89; plain OCR did not produce table structure. The existing baseline supplies no text-box locators, so none are invented for it.

Decision: defer default adoption. Original files remain unchanged; uncertain regions, missing table structure and failures remain explicit. Reproduce with `scripts/evaluate_ocr.py --workdir tests/fixtures/ocr_benchmark --out /tmp/ocr.json`, setting `NOESIS_OPTIONAL_PYTHON_PADDLEOCR` and `NOESIS_OPTIONAL_PYTHON_OCR_TESSERACT` to prepared interpreters and exposing the installed Tesseract executable/language data through `PATH`/`TESSDATA_PREFIX`. Each process has a 120-second deadline, 3 GiB RSS cap and four-page input limit. Models are downloaded explicitly through the existing pinned model registry; runtime loading uses their local paths. Evidence: `workflow-review-evidence/ocr-comparison-2026-09-09.json`.

## EMA — #1486

The live public JSON captures contained 6,742,831 bytes of medicine metadata and 29,119,743 bytes of linked-document metadata. The document index exceeded the former 20 MB ceiling; callers can now explicitly select a bounded index limit up to 100 MB, still reserved against their project byte budget. The selected Benlysta medicine and German regulatory PDF imported successfully. The English PDF initially returned HTTP 429 with Retry-After 10 seconds; a single separately budgeted retry after more than 2,700 seconds succeeded. Both language documents and both metadata observations replay offline.

Evidence: `workflow-review-evidence/ema-live-2026-09-09.json` retains the original failed English attempt, and `ema-backoff-retry-2026-09-09.json` records its successful continuation. Reproduce with existing `regional` operations `ema_medicines`, `ema_documents` (explicit `max_index_bytes` if needed), and `acquire_document` for selected returned links. This implements the documented website JSON contract, not the distinct PMS API. Centralised EMA coverage, missing documents and lack of national-authorisation completeness remain explicit; it does not claim openFDA parity. The focused tests cover native metadata, unavailable documents, source revisions, input budgets and replay. Selected PDFs retain source bytes and parser/citation locators under the shared 120-second/1 GiB parser limits.

## OpenSanctions public bulk selection — #1484

The documented non-commercial bulk path now complements credentialed hosted matching. `sanctions_dataset` requires an explicitly selected published artifact version, uses the public EU FSF file, and performs bounded exact casefolded name/alias selection locally. Query names are not sent to the provider. Hosted matching still requires a credential. The `latest` URL returned a 307 redirect; the selected immutable target was verified from its Location and the dataset's displayed version, without enabling implicit redirects in the shared transport.

Version `20260908225701-knv` supplied 16,325,268 bytes/15,938 entities in 0.70 seconds. Two German-name candidates were selected; the authored unrelated-name probe returned no candidate. This is source attribution and exact selection, not independent identity verification. Provider scores remain null because the bulk file supplies no matching score. Originating lists, native properties, dates and entity IDs survive ingestion and offline replay. Prior IDs absent from a complete selected snapshot receive an explicit revision recording dataset absence, not a global delisting or misconduct conclusion. Tests cover aliases, similar-name nonmatches, missing prior entries, updates, stable identities, replay, credential boundaries and existing review-inbox behavior. No automatic identity merge occurs.

Evidence: `workflow-review-evidence/opensanctions-bulk-2026-09-09.json`. Reproduce through the regional CLI with `operation=sanctions_dataset`, `provider=opensanctions`, `params.artifact_version`, `params.names`, and optional bounded `previous_entity_ids`. Select the current published version using the [bulk download documentation](https://www.opensanctions.org/docs/bulk/updates/) and [EU FSF dataset page](https://www.opensanctions.org/datasets/eu_fsf/); the adapter does not certify an old version as current. Each capture is limited to 20 MB and 100,000 parsed entities; name selections are limited to 20 with 100 returned new matches. Up to 100 explicitly monitored prior IDs can additionally yield presence/absence revisions. Follow the publisher's update cadence. This run was non-commercial integration evaluation; commercial data reuse requires the provider's data license. The paid hosted matching API was not executed and is not represented as a completed live comparison.

## Guardian live prerequisite — #1391, still open

A single bounded request using the public `api-key=test` example from the [official documentation](https://open-platform.theguardian.com/documentation/item) returned `authentication_failed`. Fixture conformance still passes. No credential guesses or registration followed. Evidence: `workflow-review-evidence/guardian-public-example-2026-09-09.json`. A working appropriately licensed application credential remains necessary; the public example does not establish account access or commercial reuse rights.


## Refreshed remaining acceptance — 23 open issues

GitHub was refreshed after the sixteen closures in this date’s continuation. The table below records remaining acceptance, not a claim that every native runtime comparison has already executed.

| Remaining work | Open issues | Required evidence |
| --- | --- | --- |
| Noesis human collection and review pilot | #1420, #1443, #1444, #1458, #1521 | Two independent annotators, agreement/adjudication, release approval and actual reviewer effort/error reduction. The specified collection cannot be replaced by published retrieval labels. |
| Model and quality comparisons | #1427, #1492, #1499, #1507, #1508, #1509, #1510, #1514 | Complete each issue's native baseline/candidate runs and independent task/domain judgments, including annotated scans/audio, answer support, extraction, entity review and language-identified task comparisons. Individual adapters and authored contract tests are implemented; this is not a collective acceptance certificate. |
| Credentialed provider acceptance | #1391, #1471, #1483, #1488, #1489 | Working appropriately licensed Guardian, OpenAlex content, OpenCorporates, Exa and Tavily access, and the requested live coverage/cost/quality comparisons. A GitHub token does not provide these provider accounts. |
| Tracking parents | #1348, #1399, #1401, #1406, #1469 | Completion of their remaining linked children. |

Broad verification: `.venv/bin/python -m pytest tests/unit/ingestion tests/unit/kb tests/unit/evaluation -q` completed with **1,585 passed, 13 skipped, 19 warnings**, in 102.37 seconds. Later published-retrieval selection/digest and independent metric-verification checks passed separately (three tests). All changes remain local and uncommitted.


## Initial published retrieval diagnostic — subsequently extended for #1504 and #1426

`scripts/evaluate_published_retrieval.py` now executes the actual pinned MiniLM and multilingual-E5 encoders in bounded offline workers against the same frozen 96-document corpus and 16 published test queries (eight German/eight English). All 32 runs completed; all 288 recall/nDCG/reciprocal-rank scores at 5/10/30 agree with ir-measures 0.4.3 within 1e-9. Immutable dataset digests, model revisions, document identities/revisions, labels, ranks, timings and resource limits are retained in `workflow-review-evidence/published-retrieval-2026-09-09.json`.

| Model | German recall@5 / nDCG@5 | English recall@5 / nDCG@5 | German query p50 / p95 | English query p50 / p95 | Peak worker RSS | Median corpus encoding |
| --- | --- | --- | --- | --- | --- | --- |
| MiniLM | 0.750 / 0.750 | 1.000 / 1.000 | 25.3 / 28.3 ms | 24.7 / 29.2 ms | 837.4 MiB | 3.414 s (28.13 documents/s) |
| multilingual-E5-small | 1.000 / 0.954 | 1.000 / 0.929 | 34.6 / 69.8 ms | 48.1 / 65.7 ms | 1218.8 MiB | 6.792 s (14.14 documents/s) |

Hardware: Intel Xeon W-1370, 16 logical CPUs, 15.33 GiB RAM, CPU-only workers limited to two threads, 120 seconds and 3 GiB RSS per run. Query timing includes warm query encoding and exact index search, excluding model loading and index construction; p95 uses nearest rank (the maximum for eight queries). Median model loading was 3.45/5.05 seconds respectively. Serialized representation sizes were 832,301/828,594 bytes, not full database file sizes. Indexes were isolated, model-versioned, in-memory and discarded after each run; production indexes were untouched.

This is a **short-passage diagnostic**, not a complete GermanDPR/BEIR benchmark or Noesis acceptance set. Selection takes the first eight eligible queries in each published test split before inference. Positive passages remain complete; both pinned tokenizer limits are enforced without truncation. Fifteen overlong GermanDPR hard negatives were omitted explicitly. The corpus contains 17 GermanDPR and 79 SciFact documents, with incomplete pooled judgments and a small candidate universe. GermanDPR positives derive from human GermanQuAD annotation; its hard negatives are mined, not independently adjudicated negatives. SciFact relevance includes supporting or refuting evidence and does not establish entailment. Published test examples may have appeared in model training; absence of contamination is not established.

Sources and attribution: [GermanDPR dataset card](https://huggingface.co/datasets/deepset/germandpr), Möller/Risch/Pietsch (2021), deepset and Wikipedia contributors, declares CC-BY-4.0. [BEIR SciFact distribution](https://github.com/beir-cellar/beir) and [its dataset card](https://huggingface.co/datasets/BeIR/scifact), based on Wadden et al. (2020), declare CC-BY-SA-4.0 in the card; underlying abstracts retain source rights. Original dataset text is not copied into the checked-in result artifact. The script lists exact download URLs and SHA-256 values and never executes remote dataset code.

Reproduce after explicitly acquiring the listed dataset files and pinned model snapshots: `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_published_retrieval.py --data-dir /tmp/noesis-independent-retrieval --out /tmp/published-retrieval.json`. The report records torch 2.14.0+cpu, transformers 5.16.1, sentence-transformers 5.7.0, DuckDB 1.5.5 and ir-measures 0.4.3. Runtime execution remains offline; missing weights fail closed. The E5 snapshot was explicitly fetched under a 1,200 MiB download ceiling after the smaller ceiling rejected the repository's paired weight files.

Decision: **defer production adoption**. E5 improved German ranking on this selection, scored lower on English, and roughly doubled corpus encoding time. There are no independently translated German-to-English queries or representative legal/Berlin judgments, and this semantic-only exact-index run does not exercise the deployed lexical/fusion/reranker consumers. Therefore #1504 and #1426 remain open, and these published labels do not complete or relabel #1420.


## Native hybrid service comparison — #1426 completed

The remaining four-path comparison now executes the real `LexicalSearchService`, `VectorSearchService`, `HybridRetriever` and `CrossEncoderReranker` against a fresh PostgreSQL/pgvector instance, using the frozen published GermanDPR/SciFact subset described above. All 64 runs completed. All 576 recall/nDCG/MRR checks agree with ir-measures 0.4.3. Requested output is 30, default candidate pools are 60, and semantic/fusion/reranked runs actually returned 30 results. Lexical queries correctly returned zero matches; disconnecting the lexical connection instead produced `partial` with an `InterfaceError` diagnostic and retained vector results.

Native execution exposed and repaired psycopg2-incompatible `$1` placeholders, an invalid `documents.doc_id` reference, and inconsistent document/chunk result IDs. Vector search now binds each `%s` parameter in order, reads the source ID from the chunk with legacy article-ID fallback, and returns chunk IDs consistently with lexical search. Malformed, missing, non-finite or misaligned cross-encoder scores fail explicitly; the hybrid response preserves fusion candidates with partial diagnostics. The database migrations now use valid PostgreSQL view/NOTICE syntax and safely recreate their update trigger when replayed. Both migration directories remain byte-identical.

| Mode | German recall / nDCG / MRR @10 | English recall / nDCG / MRR @10 | German p50 / p95 | English p50 / p95 |
| --- | --- | --- | --- | --- |
| Lexical | 0 / 0 / 0 | 0 / 0 / 0 | 0.90 / 5.08 ms | 0.61 / 0.97 ms |
| Semantic | 1 / 0.834 / 0.786 | 1 / 1 / 1 | 22.12 / 55.05 ms | 16.49 / 22.73 ms |
| RRF fusion | 1 / 0.834 / 0.786 | 1 / 1 / 1 | 22.14 / 56.73 ms | 16.87 / 22.77 ms |
| Reranked | 1 / 0.845 / 0.792 | 0.875 / 0.829 / 0.812 | 1715.71 / 2386.83 ms | 2463.53 / 2540.73 ms |

The existing English `plainto_tsquery` configuration requires the full query's indexed terms; it found no matches for these full natural-language questions. Thus fusion added no lexical evidence. Existing reranker fusion is preserved (0.7 original score plus 0.3 native cross-encoder score), without claiming calibration. Query timings include warm encoding where required and exclude model/index loading. Compute is local and not metered; there were no hosted inference calls. The worker completed in 48.62 seconds under its 600-second/3 GiB limit; PostgreSQL separately had 512 MiB, two CPUs and a 256 MiB temporary data volume. The container was removed after execution.

Consumer audit: repository references show no production API/MCP caller of `services/rag/retriever.py::HybridRetriever`; its convenience wrapper and tests/demos are distinct from the maintained KB query contract. `services/api/routes/ask.py` calls `services/rag/answer.py::RAGAnswerService`, whose `_perform_vector_search` still explicitly simulates sample documents. It is not represented as a measured real retrieval consumer. The maintained domain answer API in `src/api/routes/kb_routes.py` calls `src/kb/contract.py::kb_answer` / `src/kb/answer.py::build_answer`; the query MCP in `tools/knowledge_engine_mcp/server.py::_query_engine` constructs `UnifiedQueryEngine`, whose domain adapters call backing `search`/`semantic_search`. These separate paths are not silently substituted with the optional hybrid benchmark.

Evidence: `workflow-review-evidence/hybrid-native-2026-09-09.json`. Reproduce with `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_hybrid_retrieval.py --data-dir /tmp/noesis-independent-retrieval --out /tmp/hybrid.json`, after explicitly caching the image/model/dataset pins listed by the script. The script starts only a new loopback container using the pinned cached image and removes it in `finally`; the worker rejects populated or non-benchmark databases. `NOESIS_TEST_PGVECTOR=1 .venv/bin/python -m pytest tests/integration/test_native_hybrid_database.py -q` passed, verifying real migration replay, parameter binding, source/date filters, injection-shaped filter values, lexical search and zero matches. Focused contracts passed with 21 tests and one native optional skip; the separate native runs above exercise those optional dependencies.

Decision: retain defaults and defer hybrid/reranker adoption. This completes QA-02's bounded comparison and consumer audit; it does not complete #1420, certify deployment, establish entailment or remove the published-subset limitations. The earlier semantic-only section remains historical evidence.


## Report proposal comparison — #1520 completed with defer decision

The optional generator now states the full expected JSON schema in its prompt. Both constrained and unconstrained runs receive the same schema, authorized evidence, model pin and token budget; only constrained runs apply the Outlines/LLGuidance grammar. The new `report-unconstrained` worker is evaluation-only. Both modes still undergo complete schema/reference validation before a pending proposal can be stored. Workers now honor explicit bounded `max_new_tokens` rather than ignoring the requested comparison budget. Existing report revision/access checks and manual decisions remain authoritative.

The comparison runs `ReportUpdateStore.propose` for deterministic notices and the native generators for two authored DE/EN regression cases plus two proposals grounded in the independently published Berlin XML enactment-date metadata. That source is the prior hash-verified 136,850-byte capture, with SHA-256 `b701bc5b07cb78c22a9db24b39c74a15f7a896ea2b333a3002e6c41f8610fb8a` and the exact metadata locator retained. Its expected date comes from the provider, not a model label. The deliberately wrong starting assertions are marked as authored probes. This is a narrow factual-field comparison, not an independent human study of general writing quality.

Constrained generation produced schema-valid proposals in 4/4 cases; unconstrained generation produced complete valid JSON in 0/4. Only the authored English correction was incorporated. Both German and English Berlin proposals retained the incorrect year even though the captured metadata supplied the correct one, so neither satisfied the independently sourced correction expectation. Explanations also repeated or merely restated the assertion. Schema validity therefore did not establish evidence support or substantive editing utility. The deterministic notices explicitly requested review and retained the prior wording as previously reported, without claiming a corrected fact.

On the native Berlin pair, constrained workers took 39.27/38.56 seconds and peaked at 1,100,832,768/1,192,873,984 bytes RSS. Unconstrained workers failed with `truncated_or_invalid_json` after 57.84/63.20 seconds. Review notices took 14.79/15.17 ms (these timings exclude model loading because notices load no model). The tested configuration is SmolLM2-135M-Instruct at the registry pin, Outlines 1.3.3, LLGuidance 1.8.0, CPU with two worker threads, 512 output tokens, 120 seconds and 3 GiB per worker. All four successful model proposals entered pending review only; all original report revisions remained unchanged. No model edit was approved.

Evidence: `workflow-review-evidence/report-proposals-comparison-2026-09-09.json` and `report-proposals-berlin-2026-09-09.json`. Reproduce with `PYTHONPATH=/tmp/noesis-outlines-packages:.eval-packages:. /usr/bin/python scripts/evaluate_report_proposals.py --out /tmp/proposals.json`, adding `--berlin-xml /tmp/noesis-berlin-law.xml` for the frozen native case. Dependency versions are recorded in each artifact. Ten report-generator tests passed in the native Outlines environment, including grammar compatibility, invalid references, token bounds, truncation, stale revisions and unchanged manual approval gates.

Decision: **defer adoption of the tested generator** and retain deterministic notices by default. The requested baseline comparison and concrete source-supported edit review have now executed; a larger human writing-quality study would be needed to reconsider adoption, but is not represented as completed or as a prerequisite silently added to this evaluation issue. Earlier unsupported/truncated runtime attempts remain historical evidence.


## Three-direction E5 comparison — #1504 completed

The new batched retrieval evaluator builds a single isolated model-versioned index per backend and executes all subsequent queries against it. It preserves E5's mandatory passage/query prefixes, normalized vectors and 512-token limit, alongside MiniLM's 256-token limit. Inputs are selected before inference to fit both models; no passage or query is truncated. Source IDs, revisions, model spaces and source manifests are retained. No production index is modified; an E5 rollout requires a distinct reindexed space and rollback can retain the original MiniLM index. Existing mixed-space rejection remains authoritative even though both models output 384 dimensions.

The identical corpus now has 104 documents. It combines the prior GermanDPR/SciFact subset with eight distinct English XNLI premises. German XNLI hypotheses supply the eight German-to-English queries. A positive supporting-passage judgment is **derived from the published human entailment label and aligned professional translation**, not from this assistant or either retrieval model. This is supporting-evidence relevance, not a separately collected human search-relevance dataset. Model-generated labels and mismatched translations are rejected. These eight pairs are from the published test split; no new translations were generated.

| Direction | MiniLM recall / nDCG / MRR @5 | E5 recall / nDCG / MRR @5 | MiniLM p50 / p95 | E5 p50 / p95 |
| --- | --- | --- | --- | --- |
| German → German | 0.750 / 0.750 / 0.750 | 1.000 / 0.954 / 0.938 | 26.48 / 27.91 ms | 35.51 / 37.68 ms |
| English → English | 1.000 / 1.000 / 1.000 | 1.000 / 0.929 / 0.906 | 24.26 / 26.63 ms | 35.40 / 36.56 ms |
| German → English support | 0.250 / 0.188 / 0.167 | 1.000 / 1.000 / 1.000 | 25.56 / 29.26 ms | 34.53 / 38.27 ms |

All 48 queries completed and all 432 recall/nDCG/MRR values at cutoffs 5/10/30 agree with ir-measures 0.4.3. CPU encoding plus index construction processed 26.54 documents/s for MiniLM and 15.52 for E5; corpus encoding alone took 3.69/6.47 seconds. Peak worker RSS was 862,695,424/1,301,520,384 bytes, and serialized index representations were 900,396/896,411 bytes. Query timings include warm encoding and search; model/index loading is separate. The same Xeon W-1370 CPU host and two-thread workers were used. One initial MiniLM worker exceeded its 180-second deadline; the failure is retained in `cross-language-retrieval-initial-2026-09-09.json`. A bounded rerun of both backends completed in 9.88/14.48 seconds. The isolated initial timeout is not represented as a diagnosed or eliminated failure mode.

Evidence: `workflow-review-evidence/cross-language-retrieval-2026-09-09.json`. Reproduce with `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_cross_language_retrieval.py --retrieval-data-dir /tmp/noesis-independent-retrieval --xnli-data-dir /tmp/noesis-xnli --out /tmp/cross-language.json`. Dataset URLs, hashes, native row references and licenses are included in the result; XNLI is CC-BY-NC-4.0 and this is local non-commercial research evaluation. Model/dependency pins remain those in the registry and prior native retrieval report. Five targeted dataset/metric/provenance tests passed; existing E5 adapter tests cover prefixing, token limits, normalization and mixed-space rejection.

Decision: **keep E5 opt-in**. It improved this small German and cross-language selection at higher indexing/memory cost, while MiniLM ranked the English selection better. These measurements justify neither a default switch nor choosing a heavier model. Public benchmark contamination, small sample size, incomplete pooled judgments and absent legal/Berlin task-readiness coverage remain explicit. This completes the requested lower-cost three-direction evaluation; it does not replace #1420's specified collection.

## Multilingual NLI native comparison — #1507 implementation extended

Added a bounded native comparison against the pinned existing DeBERTa baseline using independently human-labelled XNLI validation/test pairs and aligned professional German/English translations. The download-only manifest records immutable dataset revision, SHA-256, sizes and CC-BY-NC-4.0 license. Selection is independent of predictions: 20 examples per class, language and split (240 pairs per model), with premise groups excluded across validation/test. Source row IDs and input hashes preserve context identity; inference rejects excessive tokens instead of truncating. Public benchmark training contamination is not excluded. Textual entailment measures support within the supplied text, not factual truth.

CPU inference now explicitly requests float32 for mDeBERTa; the checkpoint's implicit dtype made the initial run much slower. Both implicit-dtype artifacts are preserved separately. With the explicit CPU configuration, baseline/candidate completed the 240 pairs in 43.87/44.03 seconds, using peak RSS 1,243,017,216/2,505,605,120 bytes. Both used two threads on the same Xeon W-1370 host, offline pinned models, a 600-second deadline and a 3 GiB process-tree memory limit. German p50/p95 per-pair times were 159.94/197.07 ms versus 153.31/182.09 ms; English times were 148.74/171.74 ms versus 154.71/178.04 ms.

| Language | Baseline raw macro F1 / Brier | Candidate raw macro F1 / Brier | Candidate calibrated macro F1 / Brier / coverage |
| --- | --- | --- | --- |
| German | 0.8146 / 0.3218 | 0.8783 / 0.1839 | 0.8931 / 0.2105 / 0.9667 |
| English | 0.9158 / 0.1587 | 0.9667 / 0.0646 | 0.9491 / 0.0672 / 0.9667 |

The result includes per-class precision/recall/F1, confusion matrices, calibration bins and separately selected validation temperature/abstention policies. Candidate calibration worsened held-out Brier in both languages and English macro F1; these policies are not production recommendations. Batch-eight probes compare probability stability with single-pair inference. The separate transfer runner uses the existing explicit stance targets and four stance/seven frame hypothesis templates, six existing test records per task. Their labels are not certified independent EX-05 annotations, and their languages are unannotated: stance macro F1 was 0.1667/0.2667 and frame macro F1 0.5102/0.4286. These are separate diagnostic results, not established German task quality.

Two additional human-labelled XNLI reported-speech examples use a held-out premise group outside the main selection. Baseline predicts neutral in both languages; mDeBERTa incorrectly predicts entailment in German (0.573), while correctly predicting neutral in English. Reported speech is explicitly distinguished from a literal quotation challenge; the published English split does not supply quotation markers. Transfer runs completed in 10.75/11.85 seconds, at peak RSS 1,139,834,880/2,453,442,560 bytes.

Decision: retain the opt-in backend and existing defaults. Evidence: `workflow-review-evidence/published-nli-2026-09-09.json` and `nli-task-transfer-2026-09-09.json`; earlier implicit-dtype artifacts retain that qualifier in their filenames. Reproduce with `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_published_nli.py --data-dir /tmp/noesis-xnli --out /tmp/nli.json`, followed by `scripts/evaluate_nli_task_transfer.py --xnli-data-dir /tmp/noesis-xnli --out /tmp/transfer.json`. Model pins are recorded in the reports; runtime used torch 2.14.0+cpu and transformers 5.16.1. Thirteen focused dataset/mining runtime tests passed, including explicit CPU dtype and offline model-loading contracts.

Issue #1507 remains open for literal-quotation coverage and a language-identified German stance/frame comparison. These results do not complete #1420, #1443 or #1444.

## Native Qwen reranker comparison — #1506 implementation extended

The dedicated Qwen causal-LM adapter now explicitly uses float32 on CPU, requests only final-token logits, and disables the unused generation cache. Its instruction template, left padding, final-token yes/no probability, stable candidate IDs and source metadata remain intact. Over-budget prompts fail before inference without truncation. Eight native model adapter tests and sixteen reranking/fallback contract tests passed.

The new `reranker-benchmark` worker compares the actual pinned MS MARCO MiniLM cross-encoder and Qwen against exactly the first two queries in each of the three retrieval directions, using each query's original top 30 MiniLM candidates from the frozen 104-document corpus. No positive passage is injected. Source text must match its SHA-256 revision, duplicate IDs are rejected, and both corpus/query hashes must match the retrieval capture. Original public judgments and the explicitly derived XNLI supporting-passage judgments remain distinct. Twelve native query runs completed; 108 recall/nDCG/MRR values agree with ir-measures 0.4.3.

| Direction | MiniLM reranker mean nDCG @5 | Qwen mean nDCG @5 |
| --- | --- | --- |
| German → German | 0.750 | 1.000 |
| English → English | 0.500 | 1.000 |
| German → English support | 0.500 | 0.500 |

One cross-language query's relevant passage was absent from its fixed candidate pool, so neither reranker could recover it. Six queries do not establish broad multilingual quality. On the same Xeon CPU with two threads and batch size two, baseline p50/p95 was 0.940/0.992 seconds per 30 candidates (32.05 pairs/s); Qwen was 41.962/43.904 seconds (0.705 pairs/s). Worker totals were 10.34/260.89 seconds, with peak RSS 722,014,208/4,239,081,472 bytes. The original 3 GiB run rejected Qwen during model loading; its explicit memory-limit failure is archived. Both completed under the repeated 4 GiB/600-second configuration. The existing opt-in service's 15-second deadline is substantially below these measured Qwen query times and must remain an explicit failure/fallback boundary.

Main evidence: `workflow-review-evidence/native-rerankers-2026-09-09.json`, with failed budget evidence in `native-rerankers-3gib-2026-09-09.json`. Reproduce using `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_native_rerankers.py --retrieval-data-dir /tmp/noesis-independent-retrieval --xnli-data-dir /tmp/noesis-xnli --retrieval-report docs/development/workflow-review-evidence/cross-language-retrieval-2026-09-09.json --out /tmp/rerankers.json`. Registry pins and licenses are stored in the output; runtime uses torch 2.14.0+cpu, transformers 5.16.1 and sentence-transformers 5.7.0. Native relevance scores are not factual-support probabilities. The existing fallback retains retrieval candidates and diagnostics; defaults remain unchanged. Smaller candidate/batch and score-fusion probes are recorded separately when completed.

The completed smaller-pool probes use the same first query per direction, with ten unchanged candidates at batch sizes one and two. Baseline p50/p95 was 0.329/0.329 seconds (batch one) and 0.335/0.349 seconds (batch two); Qwen was 13.977/14.387 and 13.634/13.723 seconds. Qwen still used peak RSS 4,236,095,488/4,143,472,640 bytes, so reducing candidate count did not remove model memory cost. Batch-one/two maximum absolute native score differences were 2.38e-6 for MiniLM and 3.58e-7 for Qwen. These measurements compare matching query IDs; the three-query probe averages are not interchangeable with the original six-query averages. Token limits reject excess prompt input rather than manufacturing a shorter evidence span; the focused overlong-input test verifies rejection before model execution.

Reproducing the current weighted blend (0.7 retrieval score + 0.3 native reranker score) on stored native outputs reduced one German Qwen nDCG@5 from 1.000 to 0.631, leaving the other five Qwen values unchanged. Baseline values were unchanged. Native score ranges differ and were not calibrated or tuned on held-out labels, so these measurements do not justify reusing the baseline blend for Qwen. Evidence is `native-rerankers-10-batch1-2026-09-09.json`, `native-rerankers-10-batch2-2026-09-09.json` and `reranker-fusion-review-2026-09-09.json`; the latter cross-checks 216 fused metric values against ir-measures. The runner exposes `--candidate-count 10 --queries-per-direction 1 --batch-size 1` (or `2`) and records raw/fused rankings in new executions.

**#1506 evaluation completed: defer adoption, retain opt-in and explicit deadlines.** The dedicated adapter, native fixed-candidate comparison, resource/batch measurements, score-fusion check and failure tests are complete. This does not establish evidence truth, representative deployment quality or completion of the separate human evaluation collection.

## Matching cross-language hybrid reference for BGE

The native PostgreSQL harness now accepts `--xnli-data-dir` and preserves the three query directions separately. It reconstructs the identical 104-document/24-query corpus used by E5/BGE, recording both document and query-set hashes. All 96 lexical, semantic, fusion and reranked runs completed; 864 metric values agree with ir-measures. PostgreSQL was again isolated and removed after execution. Evidence: `workflow-review-evidence/hybrid-cross-language-2026-09-09.json`; reproduce the prior hybrid command with `--xnli-data-dir /tmp/noesis-xnli`.

At cutoff five, semantic/RRF nDCG was 0.750 German, 1.000 English and 0.1875 German-to-English. Reranked nDCG was 0.8452, 0.8289 and 0.2039 respectively. Lexical full-query matching again returned no matches. Fusion p50/p95 was 13.09/13.88 ms German, 12.35/13.53 ms English and 13.70/16.64 ms cross-language; reranked times were 1.420/1.933, 2.042/2.098 and 1.867/2.063 seconds. The worker completed in 51.69 seconds at peak RSS 1,068,539,904 bytes, plus the separate 512 MiB database container. These are bounded native service measurements, not deployed endpoint measurements or a representative legal/Berlin collection.

## BGE-M3 native representations and bounded indexing — #1505

Executed the pinned BGE-M3 model through FlagEmbedding's native dense, learned sparse and ColBERT multi-vector outputs on the same 104-document, 24-query corpus as the PostgreSQL comparison. Both document and query-set SHA-256 values match exactly. All 72 mode/query combinations completed; 648 recall/nDCG/MRR values agree with ir-measures. Dense+sparse uses fixed weights 1:1; multi-vector uses mean query-token MaxSim. The exact isolated index searches all 104 documents and returns at most 30. No weights were fitted to these test labels, and no production index was modified.

| Direction | BGE dense recall / nDCG @5 | BGE dense+sparse | BGE multi-vector | Existing RRF recall / nDCG @5 |
| --- | --- | --- | --- | --- |
| German → German | 1.000 / 0.954 | 1.000 / 0.954 | 1.000 / 1.000 | 0.750 / 0.750 |
| English → English | 1.000 / 1.000 | 0.875 / 0.875 | 0.875 / 0.875 | 1.000 / 1.000 |
| German → English support | 1.000 / 1.000 | 1.000 / 1.000 | 1.000 / 1.000 | 0.250 / 0.188 |

All representations are stored together for this controlled experiment. The measured serialized JSON index is 476,287,816 bytes, not an estimate of an optimized sparse or ColBERT deployment. Native encoding took 169.56 seconds, index construction 11.02 seconds, combined throughput 0.576 documents/s, total worker time 339.17 seconds and peak process-tree RSS 2,985,787,392 bytes. The database memory allowance is explicitly 1,024 MB within the worker's 4 GiB/600-second envelope. Query p50/p95 including encoding and exact scoring was 6.125/6.403 seconds German, 6.221/7.249 seconds English and 6.274/6.560 seconds cross-language. The evaluator computes every component once per query and shares those scores across mode sorts; each mode's time includes that shared cost. These are **not separately optimized single-modality latency measurements**, and the near-identical per-mode timings must not be interpreted as equal production costs. Existing RRF's corresponding times are reported in the matching native PostgreSQL section.

Native execution exposed two database memory failures and one deadline failure. They remain in `bge-retrieval-512mb-db-2026-09-09.json`, `bge-retrieval-1024mb-db-2026-09-09.json` and `bge-retrieval-repeated-components-2026-09-09.json`. The evaluator now inserts four encoded documents at a time, reads one stored payload at a time, accounts for size one record at a time, and reuses component scores across modes. The mode-ranking/streaming regression passes. The successful result uses these changes and the explicitly larger database budget; it does not erase the failures or establish large-corpus scalability.

Main evidence: `workflow-review-evidence/bge-retrieval-2026-09-09.json`, `hybrid-cross-language-2026-09-09.json` and `bge-comparison-environment-2026-09-09.json`. Reproduce with `PYTHONPATH=/tmp/noesis-bge-packages:.eval-packages:. /usr/bin/python scripts/evaluate_cross_language_retrieval.py --backends bge-m3 --retrieval-data-dir /tmp/noesis-independent-retrieval --xnli-data-dir /tmp/noesis-xnli --out /tmp/bge.json`. Model revision is `5617a9f61b028005a4858fdac845db406aefb181` (MIT). The recorded runtime uses FlagEmbedding 1.4.2, peft 0.20.0, accelerate 1.14.0, datasets 5.0.1, torch 2.14.0+cpu, transformers 5.16.1, sentence-transformers 5.7.0, DuckDB 1.5.5 and NumPy 2.4.6. CPU float32, two threads and the same Xeon W-1370 host were used. Model acquisition was an explicit download under a 2,400 MiB ceiling.

Decision: keep the adapter opt-in. The small independent human-labelled/translation-derived sample shows gains, but sparse/multi-vector modes also lose an English result and the current exact multi-representation implementation is costly. The production vector store is not claimed to support these representations. Migration requires a distinct model/configuration identity and a separately rebuilt index; rollback retains the old index and removes the isolated candidate index. Mixed spaces are rejected even when dimensions happen to agree. The separate Berlin exact-reference supplement is described below when complete; it does not replace human legal relevance judgments or #1420.

The Berlin supplement completed on twenty whole parsed articles selected before inference to fit MiniLM's 256-token limit. Documents include the original publisher's article label plus unchanged paragraph text and exact XML locators. Four queries name the publisher's article identifiers; two query `Volksvertretung` and `Fernmeldegeheimnis`. Reference targets come from publisher metadata and compound targets from exact original-text membership. These are deterministic reference-lookup judgments, **not independent human semantic-relevance annotations**. The independently human-labelled general/scientific comparison remains separate.

| Configuration | Exact article top-1 accuracy | Exact article recall @5 |
| --- | --- | --- |
| BGE dense | 0 / 4 | 1 / 4 |
| BGE dense+sparse | 2 / 4 | 3 / 4 |
| BGE multi-vector | 3 / 4 | 4 / 4 |
| Existing lexical | 4 / 4 | 4 / 4 |
| Existing RRF | 1 / 4 | 4 / 4 |
| Existing reranked | 4 / 4 | 4 / 4 |

Multi-vector, lexical, RRF and reranked retrieval recovered all exact compound matches at cutoff five; BGE dense/dense+sparse each missed one of three `Volksvertretung` matches. All 42 mode/query results completed, and 378 metric values agree with ir-measures. Both backends have identical document/query hashes. The combined BGE representation index was 37,850,833 bytes; encoding/indexing took 15.29/1.84 seconds (1.167 documents/s). BGE query p50/p95 was 0.676/0.792 seconds including shared exact component scoring, versus lexical 1.11/4.45 ms, RRF 11.70/12.16 ms and reranked 0.700/0.767 seconds. BGE worker total/peak RSS was 28.30 seconds/2,397,278,208 bytes; the hybrid worker used 10.15 seconds/925,646,848 bytes, plus its separate database container.

Evidence: `workflow-review-evidence/legal-reference-retrieval-2026-09-09.json`. Reproduce with `PYTHONPATH=/tmp/noesis-bge-packages:.eval-packages:. /usr/bin/python scripts/evaluate_legal_reference_retrieval.py --berlin-xml /tmp/noesis-berlin-law.xml --out /tmp/legal-reference.json`. The frozen publisher export URL/digest and locators are included. The initial harness rejected a twenty-document corpus because it unnecessarily required more than thirty documents; that attempt is retained in `legal-reference-small-corpus-rejection-2026-09-09.json`. The harness now accepts bounded nonempty corpora up to 200 documents, with the same populated-database refusal and explicit resource limits. Its rerun completed and removed the temporary database container.

The legal exact-reference results reinforce **defer adoption**: existing lexical search solved all four identifier lookups at substantially lower cost, while dense BGE missed three at cutoff five. Multi-vector recovery does not justify replacing the existing path on this diagnostic. No new Noesis human collection, optimized production multi-vector storage or general legal task-readiness claim is made.

MiniLM and E5 were repeated with the same updated bounded index implementation (`cross-language-retrieval-bounded-index-2026-09-09.json`). All 48 queries and 432 metric checks completed, with unchanged ranking quality. MiniLM p50/p95 was 69.71/82.78 ms German, 68.08/78.35 ms English and 55.94/64.49 ms cross-language; E5 was 57.20/59.56, 57.33/59.65 and 59.11/62.72 ms. Their combined encoding/indexing throughput was 25.48/16.00 documents/s, serialized index size 900,396/896,411 bytes, and peak RSS 857,968,640/1,275,326,464 bytes. Earlier latency measurements remain valid for their earlier index implementation; this repeat supplies the matching implementation reference.

**#1505 evaluation completed: defer adoption and retain existing defaults.** The native mode comparison, same-corpus PostgreSQL reference, Berlin identifier/compound supplement, resource failures and successful bounded rerun are recorded above. Final verification passed: 1,585 ingestion/KB/evaluation tests (13 skipped, 19 warnings, 102.37 seconds), 28 RAG tests, 17 native model/index/reranker contracts, and one real PostgreSQL integration test. Targeted Ruff checks and `git diff --check` passed. Earlier native Outlines verification passed ten tests. The full backlog remains incomplete; remaining independent human annotation/review and provider-access requirements have not been represented as implemented evidence.

## GLiNER2 extraction — #1493

Completed the bounded evaluation and retained the opt-in backend. The actual GLiNER2 2.0.0 SDK runs with Python 3.12.13, transformers 4.57.6 and torch 2.14.0+cpu; the checkpoint is `fastino/gliner2-multi-v1` at `c6296e25603e4d31f68ef8a9f4edb73421d1e45a` (Apache-2.0). Native CPU workers use two threads, a 4 GiB process-tree ceiling, and a 600-second deadline. Runtime model loading is offline. The interpreter path must preserve the virtualenv symlink rather than resolving it to the base interpreter. The first incorrect-interpreter run remains archived.

Published human token annotations supply twenty German GermEval test sentences and twenty English CrossNER politics test sentences, selected before inference with at most eighty tokens and only supported labels. Outer and nested German spans are retained. Canonical coordinates refer to the original tokens joined with spaces, not unavailable article whitespace. The shared ontology maps person/politician to person, organisation/party to organisation, and country/location to location; unsupported native types exclude a sentence rather than being silently re-labelled. Source byte sizes and SHA-256 hashes are checked before labels are read. These are small positive selections, with unknown training contamination, not the Noesis #1420 annotation release.

| Backend | German exact-span P / R / F1 | English exact-span P / R / F1 | German p50 / p95 | English p50 / p95 | Peak worker RSS |
| --- | --- | --- | --- | --- | --- |
| Existing English spaCy actor backend | .162 / .268 / .202 | .657 / .590 / .622 | 3.67 / 5.02 ms | 4.70 / 7.66 ms | 349 MiB |
| Language-configured spaCy | .742 / .561 / .639 | .645 / .607 / .626 | 3.78 / 5.29 ms | 4.80 / 7.22 ms | 514 MiB |
| GLiNER2 | .659 / .659 / .659 | .738 / .795 / .765 | 157.81 / 194.61 ms | 173.65 / 234.61 ms | 2935 MiB |

Both spaCy models are version 3.8.0, with spaCy 3.8.16 in the Python 3.14 evaluation runtime. The language-configured diagnostic includes LOC/PER labels, whereas the existing actor filter is projected to the shared ontology. This distinction is explicit; neither represents existing fine-grained political extraction. A separate twenty-sentence run preserves all nine original CrossNER political types: precision .516, recall .638, F1 .570, p50/p95 322/400 ms, peak RSS 3003 MiB. Its labels and selection differ from the three-type comparison.

Relations were evaluated separately against published CrossRE human annotations: first ten positive English politics test records of at most forty tokens with both native flags false. Every sentence receives the complete fifteen-label schema; no gold entity spans or label-presence information is supplied to inference. Exact directed endpoint/label precision is .100, recall .0208, F1 .0345 (one correct of ten predictions, forty-eight gold relations). The worker completed in 15.83 seconds at 3024 MiB peak RSS. The first native run exposed empty description-qualified relation aliases at the SDK result root. The adapter now accepts only exact declared aliases with empty lists there; unknown keys and nonempty unvalidated values fail closed. A captured native output fixture and negative regression tests cover this compatibility repair. The failed pre-fix run is retained.

The existing separate `config/extraction_schemas/` German-authority, EU-funding and Berlin-legal schemas cover authorities, programmes, organisations and legal references; `politics.json` adds the published nine-type schema. On a separately captured official Berlin constitutional paragraph, the native Berlin schema returned source-exact authority/date spans, an empty legal-reference field recorded as missing, and an unverified relation. That 8.94-second/3018-MiB run is a source-alignment and missing-field diagnostic, **not independently labelled structured-field quality**. Existing native funding examples are authored fixtures. No independent funding/legal structured-field annotations were available; no quality score is invented for them. German fine-grained domain readiness remains unestablished.

Decision: **defer production adoption**. The small entity improvement over German spaCy has substantial CPU/RAM cost, and relations perform poorly under the frozen schema. Completing this evaluation does not complete #1420 or validate unattended entity/relation assertions. Defaults remain unchanged; model confidence and source-aligned spans do not establish correctness.

Evidence: `workflow-review-evidence/published-ner-2026-09-09.json`, `published-ner-politics-2026-09-09.json`, `published-relations-2026-09-09.json`, and `gliner-berlin-2026-09-09.json`. Reproduce with `PYTHONPATH=.eval-packages:. /usr/bin/python scripts/evaluate_published_ner.py --data-dir /tmp/noesis-published-ner --gliner-python /home/ik/ChatGPT/Noesis/.venv312/bin/python --out /tmp/ner.json`; add `--politics-schema` for the separate politics task. Run `.venv312/bin/python scripts/evaluate_published_relations.py --data /tmp/noesis-published-ner/crossre-politics-test.jsonl --out /tmp/relations.json`. For the captured Berlin diagnostic use `NOESIS_OPTIONAL_PYTHON_GLINER2=/home/ik/ChatGPT/Noesis/.venv312/bin/python .venv/bin/python scripts/evaluate_gliner_berlin.py --xml /tmp/noesis-berlin-law.xml --out /tmp/berlin.json`.

Source attribution and access: [GermEval 2014 data documentation](https://sites.google.com/site/germeval2014ner/data) declares CC-BY-4.0. The captured mirror is pinned and is not represented as the later encoding-corrected distribution. [CrossNER](https://github.com/zliucr/CrossNER), Liu et al. (2021), supplies the original political labels; dataset redistribution rights were not established in this capture. [CrossRE](https://github.com/mainlp/CrossRE), Bassignana and Plank (2022), supplies separate relation annotations and declares GPL-3.0 for its repository; underlying source rights still apply. Raw corpora are not redistributed. Model/API setup follows the [GLiNER2 library](https://github.com/fastino-ai/GLiNER2) and the [selected model card](https://huggingface.co/fastino/gliner2-multi-v1); newer checkpoints are not substituted.

Post-extraction broad validation: **1,598 passed, 13 skipped, 19 warnings**, 96.96 seconds. The 28 focused published-extraction/schema/runtime tests also passed; targeted Ruff and `git diff --check` passed. GitHub closure of #1493 was verified, leaving 23 open issues.
