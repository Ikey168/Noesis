# Knowledge Memory MCP

Noesis memory is typed knowledge, not an unscoped key-value graph. Every record
has a namespace, tenant and optional task, provenance/evidence, epistemic status,
validity, sensitivity, and lifecycle. Corrections and consolidation preserve
history. Standard MCP memory import/export includes an explicit mapping-loss and
conflict report.
