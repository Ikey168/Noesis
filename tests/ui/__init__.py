"""UI test package.

The Streamlit UI and dashboard were removed, so the former UI/dashboard test
modules are gone. Only backend-oriented service tests remain here
(``test_core_services.py``). This package init intentionally imports nothing so
collection never depends on the removed modules.
"""
