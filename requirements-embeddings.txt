# Embedding Provider Dependencies
# Issue #228: Embedding provider (local + cloud pluggable)

# Core embedding dependencies
numpy>=1.21.0
sentence-transformers>=2.0.0

# Optional: OpenAI provider (cloud backend)
openai>=1.0.0

# For deterministic behavior
torch>=2.0.0

# Testing
pytest>=7.0.0
pytest-mock>=3.15.1
