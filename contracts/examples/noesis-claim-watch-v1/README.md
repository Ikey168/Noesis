# Claim Watch v1 examples

- `valid-watch.json` is an active topic watch.
- `valid-poll.json` demonstrates one cursor-polled, evidence-backed event.
- `invalid-event-without-evidence.json` is intentionally rejected because
  every fired event must explain itself with evidence.
