{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://noesis.local/schemas/noesis-market-quality-quarantine-v1.json",
  "title": "Noesis market source quarantine and review result v1",
  "oneOf": [
    {"$ref": "#/definitions/decision"},
    {"$ref": "#/definitions/list"}
  ],
  "definitions": {
    "decision": {
      "type": "object",
      "required": ["contract", "namespace", "object_kind", "revision_id", "status", "reason", "reviewer_id", "updated_at_ms"],
      "properties": {
        "contract": {"const": "noesis-market-quality-quarantine-v1"},
        "namespace": {"type": "string", "minLength": 1},
        "object_kind": {"enum": ["price_bar", "corporate_action", "financial_fact"]},
        "revision_id": {"type": "string", "minLength": 1},
        "status": {"enum": ["quarantined", "released"]},
        "reason": {"type": "string", "minLength": 1},
        "finding_ids": {"type": "array", "items": {"type": "string"}},
        "reviewer_id": {"type": "string", "minLength": 1},
        "created_at_ms": {"type": "integer", "minimum": 0},
        "updated_at_ms": {"type": "integer", "minimum": 0},
        "idempotent": {"type": "boolean"}
      },
      "additionalProperties": false
    },
    "list": {
      "type": "object",
      "required": ["contract", "namespace", "items", "limit"],
      "properties": {
        "contract": {"const": "noesis-market-quality-quarantine-v1"},
        "namespace": {"type": "string", "minLength": 1},
        "items": {"type": "array", "items": {"$ref": "#/definitions/item"}},
        "limit": {"type": "integer", "minimum": 1, "maximum": 500}
      },
      "additionalProperties": false
    },
    "item": {
      "type": "object",
      "required": ["object_kind", "revision_id", "status", "reason", "finding_ids", "reviewer_id", "created_at_ms", "updated_at_ms"],
      "properties": {
        "object_kind": {"enum": ["price_bar", "corporate_action", "financial_fact"]},
        "revision_id": {"type": "string", "minLength": 1},
        "status": {"enum": ["quarantined", "released"]},
        "reason": {"type": "string", "minLength": 1},
        "finding_ids": {"type": "array", "items": {"type": "string"}},
        "reviewer_id": {"type": "string", "minLength": 1},
        "created_at_ms": {"type": "integer", "minimum": 0},
        "updated_at_ms": {"type": "integer", "minimum": 0}
      },
      "additionalProperties": false
    }
  }
}
